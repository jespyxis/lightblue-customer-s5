server:
  context-path: /micro
  port: 8080

spring:
  cloudant:
    username: ${vcap.services.Cloudant-s5.credentials.username}
    password: ${vcap.services.Cloudant-s5.credentials.password}
    host: ${vcap.services.Cloudant-s5.credentials.host}
    protocol: https
    port: 443
    database: customers
